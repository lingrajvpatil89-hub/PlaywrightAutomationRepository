let x = "b";
 
switch (x) {
  case "a" || "e" || "i" || "o" || "u" || "A" || "E" || "I" || "O" || "U":
    console.log(x + " is a Vowel");
    break;
  default:
    console.log(x + " is not a Vowel");
}
 